# resumerbot

Minimal Flask UI scaffold.

Run locally:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python run.py
```
Python bot that turns Job Descriptions into fine-tuned resumes, in docx formats. 
